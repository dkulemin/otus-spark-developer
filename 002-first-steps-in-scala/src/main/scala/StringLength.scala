object StringLength {
  /**
   * Создайте программу, которая принимает строку и выводит её длину.
   * */

  def main(args: Array[String]): Unit = {
    println(scala.io.StdIn.readLine().length)
  }
}
